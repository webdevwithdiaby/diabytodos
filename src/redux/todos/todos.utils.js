/*export const updateCompletedState = (state, todo) => {
  return {
    ...state,
    todos: {
      allIds: [...state.todos.allIds],
      byId: {
        ...state.todos.byId,
        [todo.id]: { ...todo, isCompleted: !todo.isCompleted },
      },
    },
  };
};*/
